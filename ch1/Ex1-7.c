/* Exercise 1-7. Write a program to print the value of EOF. */

#include <stdio.h>

/* prints the value of EOF, i.e. end-of-file */
int main()
{
  printf("The value of EOF is %d\n\n", EOF);

  return 0;
}
